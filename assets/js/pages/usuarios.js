/**
 * usuarios.js
 * Lógica específica para la vista de Usuarios
 */

document.addEventListener('DOMContentLoaded', () => {
    // La inicialización global de Select2 ya se encarga de los selects pre-renderizados
    // en esta vista (incluyendo los selects de roles y preguntas de seguridad en los modales).
});
